package com.example.practice.car_domain.car_function.abstraction;

import com.example.practice.car_domain.car.object.Car;

public interface CarGoFunction { // 자동차 고유의 세부적인 하나의 기능

    void goAhead(Car car);
}
